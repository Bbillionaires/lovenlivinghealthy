# Stripe (required for checkout, the account portal, and webhooks)
# Get these from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Get this from the Stripe CLI (`stripe listen --forward-to localhost:3000/api/webhook`)
# or from the webhook endpoint you create in the Stripe Dashboard.
STRIPE_WEBHOOK_SECRET=whsec_...
